version: '3.7'

services:
   server:
     build:
       context: server
       dockerfile: Dockerfile
     expose:
       - "5010"
     ports:
       - "5010:5010"

   client:
    build:
      context: client
      dockerfile: Dockerfile
    expose:
      - "3000"
    ports:
      - "3000:80"
